[global]
    map to guest = Bad User
    server min protocol = SMB2
    server max protocol = SMB3

[media]
    path = /shared
    browsable = yes
    read only = no
    guest ok = no
    valid users = media
    force user = 1000
    force group = 1000
    create mask = 0664
    directory mask = 0775
